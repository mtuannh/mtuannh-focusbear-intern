function calculateScoreWithBonus(score) {
  const bonus = score * 0.1;
    return score + bonus;
}

console.log(calculateScoreWithBonus(100));
console.log(calculateScoreWithBonus(100));
console.log(calculateScoreWithBonus(100));